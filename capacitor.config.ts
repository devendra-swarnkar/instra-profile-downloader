import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Instra profile downloader',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
